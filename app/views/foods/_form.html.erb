<%= form_for(food) do |f| %>
  <% if food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label 'Proteins [grams per 100g]' %>
    <%= f.number_field :prots %>
  </div>

  <div class="field">
    <%= f.label 'Carbohydrates [grams per 100g]' %>
    <%= f.number_field :carbs %>
  </div>

  <div class="field">
    <%= f.label 'Fats [grams per 100g]' %>
    <%= f.number_field :fats %>
  </div>

  <div class="field">
    <%= f.label 'Calories [kcal per 100g]' %>
    <%= f.number_field :cals %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
